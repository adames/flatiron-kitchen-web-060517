<%= form_for @recipe do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
    <%= collection_check_boxes(:recipe, :ingredient_ids, Ingredient.all, :id, :name) do |i| %>
      <p><%= i.label  { i.check_box + i.text } %></p>
    <% end %>
  <%= f.submit %>
<% end %>
